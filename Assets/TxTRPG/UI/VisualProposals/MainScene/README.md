# Main Scene Visual Proposal Assets

이 디렉터리의 스타일 자산은 `MainScene_VisualProposal.unity`에서만 사용합니다.
공용 UI 프리팹이나 `TMP_MainScene.unity`의 기본 외형을 변경하지 않습니다.

`Tools > TxT RPG > UI > Visual Proposals > Rebuild Main Scene Visual Proposal`을 실행하면
생성기가 이 디렉터리에서 담당하는 스타일 값을 현재 제안안으로 갱신합니다.
